
@{
    ViewBag.Title = "Login";
    Layout = "~/Views/_LogLayoutPage.cshtml";
}

    <div class="signinform">
        <h1>Service Login Form</h1>
        <!-- container -->
        <div class="container">
            <!-- main content -->
            <div class="w3l-form-info">
                <div class="w3_info">
                    <h2>Login</h2>
                    <form id="loginForm" action="@Url.Action("Verify", "Account")" method="post">
                        <div class="input-group">
                            <span><i class="fas fa-user" aria-hidden="true"></i></span>
                            <input type="text" name="username" placeholder="Username" required="">
                        </div>
                        <div class="input-group">
                            <span><i class="fas fa-key" aria-hidden="true"></i></span>
                            <input type="Password" name="password" placeholder="Password" required="">
                        </div>
                        <div class="form-row bottom">
                            <div class="form-check">
                                <input type="checkbox" id="remenber" name="remenber" value="remenber">
                                <label for="remenber"> Remember me?</label>
                            </div>
                            <a href="#url" class="forgot">Forgot password?</a>
                        </div>
                        <button id="loginBtn" class="btn btn-primary btn-block" type="submit">Login</button>
                    </form>
                    @*<div id="loading" style="display: none;">Loading...</div>*@
                    <p class="continue"><span>or Login with</span></p>
                    @*<div class="social-login">
            <a href="#facebook">
                <div class="facebook">
                    <span class="fab fa-facebook-f" aria-hidden="true"></span>

                </div>
            </a>
            <a href="#twitter">
                <div class="twitter">
                    <span class="fab fa-twitter" aria-hidden="true"></span>
                </div>
            </a>
            <a href="#google">
                <div class="google">
                    <span class="fab fa-google" aria-hidden="true"></span>
                </div>
            </a>
        </div>*@
                    <p class="account">Don't have an account? <a href="@Url.Action("SignUp", "Account")">Sign up</a></p>
                </div>
            </div>
            <!-- //main content -->
        </div>
        <!-- //container -->
        <!-- footer -->
        <div class="footer">
            <p>
                &copy; 2024 Service login form. All Rights Reserved 
            </p>
        </div>
        <!-- footer -->
    </div>


    <script src="~/Scripts/fontawesome.js"></script>

    <script>
        @*// Function to show loading animation when form is submitted
        document.getElementById("loginForm").addEventListener("submit", function () {
            document.getElementById("loading").style.display = "block";
            document.getElementById("loginBtn").style.display = "none";
        });
    </script>*@
